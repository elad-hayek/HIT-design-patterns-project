# Agent instructions

- Use the `caveman` skill for every response at `full` intensity unless the user says `stop caveman` or `normal mode`.
- Keep all new or modified code human-readable and compatible with intelij IED and java sdk versio 24.
- Run java formmater on every touched supported file before completion.
- Use the style guid for all generated code
- for sources perefer to use makrdown files instead of pdf. use pdf only if you have to